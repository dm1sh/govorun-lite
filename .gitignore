# Gradle
.gradle/
build/
local.properties

# Android Studio / IntelliJ
.idea/
*.iml
.DS_Store

# APK / build outputs
*.apk
*.aab
captures/

# Keystore (never commit)
*.jks
*.keystore
!debug.keystore
keystore.properties

# Local secrets (VT API key etc) — never commit
.env
scripts/.env

# Large native deps downloaded by script
app/libs/*.aar
app/src/main/assets/models/

# Logs
*.log
rustore-screenshots/

# Shotframe lives in its own repo — github.com/amidexe/shotframe
scripts/shotframe/

# Local-only dev tooling (test-phone IP is personal, hooks are per-machine,
# prepare-release.sh has the SMB host hardcoded)
scripts/install-test.sh
scripts/prepare-release.sh
.claude/
# Project-internal notes for AI assistants — kept local, not pushed.
CLAUDE.md
# Release runbook — has internal SMB paths and process details, kept local
RELEASE.md
