<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.GovorunLite" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="switchStyle">@style/Widget.Material3.CompoundButton.MaterialSwitch</item>
        <item name="android:windowBackground">?attr/colorSurface</item>
    </style>

    <!-- InputMethodService does not inherit the Activity theme. MaterialButton
         requires a Theme.MaterialComponents descendant, so the IME creates its
         controls with this dedicated wrapper theme. -->
    <style name="Theme.GovorunIme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowActionModeOverlay">true</item>
        <item name="android:windowBackground">?attr/colorSurface</item>
    </style>
</resources>
