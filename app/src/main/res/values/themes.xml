<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.GovorunLite" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="switchStyle">@style/Widget.Material3.CompoundButton.MaterialSwitch</item>
        <item name="android:windowBackground">?attr/colorSurface</item>
    </style>

    <!-- InputMethodService does not inherit the Activity theme. Use the same
         Material 3 theme as the rest of the app through a ContextThemeWrapper
         before creating MaterialButton instances. -->
    <style name="Theme.GovorunIme" parent="Theme.GovorunLite" />
</resources>
